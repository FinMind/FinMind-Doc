variables:
  GIT_STGIT_STRATEGY: fetch

stages:
  - deploy

deploy:githubpage:
  stage: deploy
  before_script:
    - make install-python-evn
  script:
    - mkdocs build
    - python gen_release_note.py
    - make deploy-github-page
  only:
    - master
    - schedules
  tags:
    - docker