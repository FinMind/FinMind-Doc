variables:
  GIT_STGIT_STRATEGY: fetch

stages:
  - deploy

deploy:githubpage:
  stage: deploy
  script:
    - make deploy-github-page
  only:
    - master
    - schedules
  tags:
    - docker